<uwpcControls:UniversalPage
    x:Class="ActionNote.App.Views.EditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ActionNote.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uwpcControls="using:UWPCore.Framework.Controls"
    xmlns:uwpcConverters="using:UWPCore.Framework.Converters"
    xmlns:commonConverters="using:ActionNote.Common.Converters"
    mc:Ignorable="d"
    x:Name="self">

    <Page.Resources>
        <uwpcConverters:BooleanToVisibilityConverter x:Key="boolToVisConv" />
        <commonConverters:ColorCategoryToImageConverter x:Key="colorImageConv" />
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <uwpcControls:PageHeader>
            <uwpcControls:PageHeader.HeaderContent>
                <TextBlock Text="{Binding PageTitle}" Style="{ThemeResource PageTitleTextBlockStyle}" TextWrapping="NoWrap"/>
            </uwpcControls:PageHeader.HeaderContent>
        </uwpcControls:PageHeader>

        <CommandBar x:Name="bottomBar" Grid.Row="2" VerticalAlignment="Bottom" HorizontalContentAlignment="Stretch">
            <AppBarButton Icon="Delete" Label="Delete" Command="{Binding RemoveCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource boolToVisConv}}" CommandParameter="{Binding SelectedNote}"/>
            <AppBarButton Icon="Clear" Label="Discard" Command="{Binding DiscardCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource boolToVisConv}, ConverterParameter=!}" CommandParameter="{Binding SelectedNote}"/>
            <AppBarButton Icon="Clear" Label="Discard changes" Command="{Binding DiscardCommand}" Visibility="{Binding IsEditMode, Converter={StaticResource boolToVisConv}}" />
            <AppBarButton Icon="Save" Label="Save" Command="{Binding SaveCommand}" CommandParameter="{Binding SelectedNote}"/>
            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Clear all" Command="{Binding ClearCommand}"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>

        <Grid Grid.Row="1" EntranceNavigationTransitionInfo.IsTargetElement="True">
            <ScrollViewer>
                <StackPanel>
                    <StackPanel DataContext="{Binding SelectedNote}">

                        <TextBox PlaceholderText="Title" Text="{Binding Title, Mode=TwoWay}" Margin="4" />
                        <TextBox PlaceholderText="Content..." Text="{Binding Content, Mode=TwoWay}" AcceptsReturn="True" Height="200" Margin="4" />

                        <ComboBox ItemsSource="{Binding ElementName=self, Path=DataContext.ColorEnumSource.ItemsSource}" SelectedItem="{Binding Color, Mode=TwoWay}" HorizontalAlignment="Stretch" Margin="4">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Converter={StaticResource colorImageConv}}" Width="32" Height="32" Margin="4" />
                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="4" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Select attachement" Command="{Binding SelectAttachementCommand}" CommandParameter="{Binding SelectedNote}" Margin="4" HorizontalAlignment="Stretch" />
                        <Button Grid.Column="1" Content="Remove attachement" Command="{Binding RemoveAttachementCommand}" CommandParameter="{Binding SelectedNote}" Margin="4" HorizontalAlignment="Stretch" />
                    </Grid>

                    <Image DataContext="{Binding SelectedNote}" Source="{Binding AttachementImage}" HorizontalAlignment="Stretch" Stretch="UniformToFill"  Margin="4" />

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</uwpcControls:UniversalPage>
