<uwpcControls:UniversalPage
    x:Class="ActionNote.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ActionNote.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uwpcControls="using:UWPCore.Framework.Controls"
    xmlns:uwpcViewModels="using:ActionNote.App.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <uwpcViewModels:SettingsViewModel />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <uwpcControls:PageHeader>
            <uwpcControls:PageHeader.HeaderContent>
                <TextBlock Text="SETTINGS" Style="{ThemeResource PageTitleTextBlockStyle}" TextWrapping="NoWrap"/>
            </uwpcControls:PageHeader.HeaderContent>
        </uwpcControls:PageHeader>

        <Grid Grid.Row="1" EntranceNavigationTransitionInfo.IsTargetElement="True">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="16">
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="General" Margin="4" />
                    <ToggleSwitch Header="Auto save notes when BACK was pressed" IsOn="{Binding SaveNoteOnBack, Mode=TwoWay}" Margin="4" />
                    <ToggleSwitch Header="Enable QuickNotes in action center" IsOn="{Binding QuickNotesEnabled, Mode=TwoWay}" Margin="4" />
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Action Center" Margin="4" />
                    <ToggleSwitch Header="Allow to remove a note" IsOn="{Binding AllowRemoveNotes, Mode=TwoWay}" Margin="4" />
                    <ToggleSwitch Header="Allow to clear all notes" IsOn="{Binding AllowClearNotes, Mode=TwoWay}" Margin="4" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</uwpcControls:UniversalPage>
